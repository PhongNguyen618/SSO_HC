# ============================================================
# SSO_HC - File cấu hình môi trường (EXAMPLE)
# Copy file này thành .env và điền giá trị thật trước khi chạy
# ============================================================

# --- Strava API ---
# Lấy tại: https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here
STRAVA_CLUB_ID=your_club_id_here

# --- App URL (quan trọng cho Strava OAuth) ---
# Phải khớp chính xác với “Authorization Callback Domain” trong Strava App settings
# Docker / production : https://ssohc.ppt-sso.com
# Dev local          : http://localhost:8008
APP_URL=https://ssohc.ppt-sso.com

# --- Webhook ---
# Token xác minh Strava Webhook (có thể đặt tuỳ ý, dùng chuỗi ngẫu nhiên)
STRAVA_WEBHOOK_VERIFY_TOKEN=change_this_to_a_long_random_webhook_token
# Tùy chọn: nếu biết subscription ID, đặt để từ chối webhook từ subscription khác
STRAVA_WEBHOOK_SUBSCRIPTION_ID=

# --- Tài khoản Admin mặc định ---
# Chỉ dùng cho lần khởi tạo CSDL đầu tiên - hãy đổi mật khẩu sau khi deploy
DEFAULT_ADMIN_USER=admin
# Bắt buộc đặt mật khẩu mạnh; không dùng admin/admin
DEFAULT_ADMIN_PASSWORD=change_this_to_a_long_random_password

# --- Cơ sở dữ liệu ---
# Docker : sqlite:////app/data/SSO_HC.db  (tương ứng volume mount ./data:/app/data)
# Local  : sqlite:///./SSO_HC.db     (chạy trực tiếp bằng run.bat)
DATABASE_URL=sqlite:////app/data/SSO_HC.db
